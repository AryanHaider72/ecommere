export interface RequestStoreAddData {
  email: string;
  cityID: string;
  storeName: string;
  description: string;
}
export interface ResponseStoreAddData {
  status: string;
  message?: string;
}
